#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int l,b,a,p;
    scanf("%d%d%d%d",&l,&b,&a,&p);
    p=2*(l+b);
    a=l*b;
    printf("%d\n%d\n",p,a);    
    return 0;
}
