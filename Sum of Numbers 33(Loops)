#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
#include <stdio.h>
int main() {
    int n,s1=0;
    while(1)
    {
        scanf("%d",&n);
        if(n==-1)
        {
            break;
        }
        s1=s1+n;
    }
    printf("%d",s1);
return 0;
}
